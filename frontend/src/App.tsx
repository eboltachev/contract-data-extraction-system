import {useState} from 'react'; import {createJob, getJob, Job} from './api/client'; import {FileUploadCard} from './components/FileUploadCard'; import {ProgressPanel} from './components/ProgressPanel'; import {AgentTimeline} from './components/AgentTimeline'; import {DownloadCard} from './components/DownloadCard';
export default function App(){const [contract,setContract]=useState<File|null>(null),[template,setTemplate]=useState<File|null>(null),[job,setJob]=useState<Job|null>(null),[events,setEvents]=useState<any[]>([]),[busy,setBusy]=useState(false);
async function start(){if(!contract||!template)return; setBusy(true); const j=await createJob(contract,template); const initial=await getJob(j.job_id); setJob(initial); const es=new EventSource(`/api/v1/jobs/${j.job_id}/events`); es.onmessage=async ev=>{const data=JSON.parse(ev.data); setEvents(x=>[...x,data]); setJob(await getJob(j.job_id)); if(['completed','failed','rolled_back'].includes(data.status)){es.close(); setBusy(false);}}; es.onerror=()=>{es.close(); setBusy(false);};}
return <main><header><h1>Автоматизированная система извлечения данных из коммерческих договоров</h1><p>Загрузите договор и Excel-шаблон — система заполнит значения по динамическим критериям.</p></header><section className="grid"><FileUploadCard title="Договор (.doc/.docx)" accept=".doc,.docx" file={contract} onChange={setContract}/><FileUploadCard title="Excel-шаблон (.xls/.xlsx)" accept=".xls,.xlsx" file={template} onChange={setTemplate}/></section><button className="btn primary" disabled={!contract||!template||busy} onClick={start}>Запустить обработку</button>{job&&<ProgressPanel progress={job.progress} status={job.status} action={job.current_action} error={job.error}/>}<AgentTimeline events={events}/><DownloadCard jobId={job?.job_id??null} ready={job?.status==='completed'}/></main>}
